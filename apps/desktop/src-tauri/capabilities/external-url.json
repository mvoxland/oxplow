{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "external-url",
  "description": "Empty capability for external-URL webview windows. No oxplow commands, no plugin access — these webviews behave like sandboxed browser tabs. The label pattern matches windows opened via `open_external_url` (label = ext-url-<random>).",
  "windows": ["ext-url-*"],
  "permissions": []
}
